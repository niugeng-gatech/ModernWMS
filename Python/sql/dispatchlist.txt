CREATE TABLE [dbo].[new_dispatchlist] (
 id INT IDENTITY(1,1) PRIMARY KEY,
 dispatch_no NVARCHAR(MAX) NOT NULL,
    dispatch_status tinyint NOT NULL,
    customer_id INT NOT NULL,
    customer_name NVARCHAR(MAX) NOT NULL,
    sku_id INT NOT NULL,
    qty INT NOT NULL,
    weight decimal NOT NULL,
    volume decimal NOT NULL,
    creator NVARCHAR(MAX) NOT NULL,
    create_time datetime2 DEFAULT GETDATE(),
    damage_qty INT NOT NULL,
    lock_qty INT NOT NULL,
    picked_qty INT NOT NULL,
    intrasit_qty INT NOT NULL,
    package_qty INT NOT NULL,
    weighing_qty INT NOT NULL,
    actual_qty INT NOT NULL,
    sign_qty INT NOT NULL,
    package_no NVARCHAR(MAX) NOT NULL,
    package_person NVARCHAR(MAX) NOT NULL,
    package_time datetime2 DEFAULT GETDATE(),
    weighing_no NVARCHAR(MAX) NOT NULL,
    weighing_person NVARCHAR(MAX) NOT NULL,
    weighing_weight decimal NOT NULL,
    waybill_no NVARCHAR(MAX) NOT NULL,
    carrier NVARCHAR(MAX) NOT NULL,
    freightfee decimal NOT NULL,
    last_update_time datetime2 DEFAULT GETDATE(),
    tenant_id bigint NOT NULL
);

-- Step 2: Copy data from the old table to the new table
INSERT INTO [dbo].[new_dispatchlist] (dispatch_no, dispatch_status, customer_id, customer_name, sku_id, qty, weight, volume, creator, create_time, damage_qty, lock_qty, picked_qty, intrasit_qty, package_qty, weighing_qty, actual_qty, sign_qty, package_no, package_person, package_time, weighing_no, weighing_person, weighing_weight, waybill_no, carrier, freightfee, last_update_time, tenant_id)
SELECT dispatch_no, dispatch_status, customer_id, customer_name, sku_id, qty, weight, volume, creator, create_time, damage_qty, lock_qty, picked_qty, intrasit_qty, package_qty, weighing_qty, actual_qty, sign_qty, package_no, package_person, package_time, weighing_no, weighing_person, weighing_weight, waybill_no, carrier, freightfee, last_update_time, tenant_id
FROM [dbo].[dispatchlist];

-- Step 3: Drop the old table
DROP TABLE [dbo].[dispatchlist];

-- Step 4: Rename the new table to match the old table's name
EXEC sp_rename '[dbo].[new_dispatchlist]', 'dispatchlist';







    dispatch_no NVARCHAR(MAX) NOT NULL,
    dispatch_status INTEGER NOT NULL,
    customer_id INTEGER NOT NULL,
    customer_name NVARCHAR(MAX) NOT NULL,
    sku_id INTEGER NOT NULL,
    qty INTEGER NOT NULL,
    weight NVARCHAR(MAX) NOT NULL,
    volume NVARCHAR(MAX) NOT NULL,
    creator NVARCHAR(MAX) NOT NULL,
    create_time NVARCHAR(MAX) NOT NULL,
    damage_qty INTEGER NOT NULL,
    lock_qty INTEGER NOT NULL,
    picked_qty INTEGER NOT NULL,
    intrasit_qty INTEGER NOT NULL,
    package_qty INTEGER NOT NULL,
    weighing_qty INTEGER NOT NULL,
    actual_qty INTEGER NOT NULL,
    sign_qty INTEGER NOT NULL,
    package_no NVARCHAR(MAX) NOT NULL,
    package_person NVARCHAR(MAX) NOT NULL,
    package_time NVARCHAR(MAX) NOT NULL,
    weighing_no NVARCHAR(MAX) NOT NULL,
    weighing_person NVARCHAR(MAX) NOT NULL,
    weighing_weight NVARCHAR(MAX) NOT NULL,
    waybill_no NVARCHAR(MAX) NOT NULL,
    carrier NVARCHAR(MAX) NOT NULL,
    freightfee NVARCHAR(MAX) NOT NULL,
    last_update_time NVARCHAR(MAX) NOT NULL,
    tenant_id INTEGER NOT NULL,
 serial_number NVARCHAR(MAX)
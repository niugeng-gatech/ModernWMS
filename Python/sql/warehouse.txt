CREATE TABLE [dbo].[new_warehouse] (
 id INT IDENTITY(1,1) PRIMARY KEY,
 warehouse_name NVARCHAR(MAX) NOT NULL,
    city NVARCHAR(MAX) NOT NULL,
    address NVARCHAR(MAX) NOT NULL,
    email NVARCHAR(MAX) NOT NULL,
    manager NVARCHAR(MAX) NOT NULL,
    contact_tel NVARCHAR(MAX) NOT NULL,
    creator NVARCHAR(MAX) NOT NULL,
    create_time datetime2 DEFAULT GETDATE(),
    last_update_time datetime2 DEFAULT GETDATE(),
    is_valid bit NOT NULL,
    tenant_id bigint NOT NULL
);

-- Step 2: Copy data from the old table to the new table
INSERT INTO [dbo].[new_warehouse] (warehouse_name, city, address, email, manager, contact_tel, creator, create_time, last_update_time, is_valid, tenant_id)
SELECT warehouse_name, city, address, email, manager, contact_tel, creator, create_time, last_update_time, is_valid, tenant_id
FROM [dbo].[warehouse] ORDER BY id ASC;

-- Step 3: Drop the old table
DROP TABLE [dbo].[warehouse];

-- Step 4: Rename the new table to match the old table's name
EXEC sp_rename '[dbo].[new_warehouse]', 'warehouse';


    warehouse_name NVARCHAR(MAX) NOT NULL,
    city NVARCHAR(MAX) NOT NULL,
    address NVARCHAR(MAX) NOT NULL,
    email NVARCHAR(MAX) NOT NULL,
    manager NVARCHAR(MAX) NOT NULL,
    contact_tel NVARCHAR(MAX) NOT NULL,
    creator NVARCHAR(MAX) NOT NULL,
    create_time NVARCHAR(MAX) NOT NULL,
    last_update_time NVARCHAR(MAX) NOT NULL,
    is_valid int NOT NULL,
    tenant_id int NOT NULL
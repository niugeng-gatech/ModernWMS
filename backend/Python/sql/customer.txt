CREATE TABLE [dbo].[new_customer] (
 id INT IDENTITY(1,1) PRIMARY KEY,
 customer_name NVARCHAR(MAX) NOT NULL,
    city NVARCHAR(MAX) NOT NULL,
    address NVARCHAR(MAX) NOT NULL,
    email NVARCHAR(MAX) NOT NULL,
    manager NVARCHAR(MAX) NOT NULL,
    contact_tel NVARCHAR(MAX) NOT NULL,
    creator NVARCHAR(MAX) NOT NULL,
    create_time datetime2 DEFAULT GETDATE(),
    last_update_time datetime2 DEFAULT GETDATE(),
    is_valid bit NOT NULL,
    tenant_id bigint NOT NULL
);

-- Step 2: Copy data from the old table to the new table
INSERT INTO [dbo].[new_customer] (customer_name, city, address, email, manager, contact_tel, creator, create_time, last_update_time, is_valid, tenant_id)
SELECT customer_name, city, address, email, manager, contact_tel, creator, create_time, last_update_time, is_valid, tenant_id
FROM [dbo].[customer];

-- Step 3: Drop the old table
DROP TABLE [dbo].[customer];

-- Step 4: Rename the new table to match the old table's name
EXEC sp_rename '[dbo].[new_customer]', 'customer';




    customer_name TEXT NOT NULL,
    city TEXT NOT NULL,
    address TEXT NOT NULL,
    email TEXT NOT NULL,
    manager TEXT NOT NULL,
    contact_tel TEXT NOT NULL,
    creator TEXT NOT NULL,
    create_time TEXT NOT NULL,
    last_update_time TEXT NOT NULL,
    is_valid INTEGER NOT NULL,
    tenant_id INTEGER NOT NULL